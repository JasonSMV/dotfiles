Use this folder for ExplorerPatcher settings that are imported or clicked manually.

Suggested contents:

- Exported `.reg` files for preferred taskbar/alt-tab behavior.
- Step notes for options that cannot be exported.
- Screenshots for visual toggles.

If you add `.reg` files here, test them in a VM or restore point first.

Check: https://github.com/Louis047/Windows-Dots/blob/main/ExplorerPatcher/simple_alt_tab.reg
